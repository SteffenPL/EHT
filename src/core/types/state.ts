/**
 * Core state definitions.
 * Now kept minimal as specific state is defined by models.
 */

// Placeholder for generic state constraints if needed
export interface BaseSimulationState {
  t: number;
  step_count: number;
}
